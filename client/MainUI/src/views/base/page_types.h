#ifndef PAGE_TYPES_H
#define PAGE_TYPES_H

// 페이지의 종류를 명확한 이름으로 정의
enum class PageType {
    Login,
    Dashboard,
    PartnerManage,
    ScmManage,
    Delivery,
    Process,
    Manufacture
    // 새로운 페이지가 추가되면 여기에 이름만 추가해야 함
};

#endif
